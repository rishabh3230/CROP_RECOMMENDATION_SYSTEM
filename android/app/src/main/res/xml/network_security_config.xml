<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Allow cleartext HTTP only for local/LAN backend addresses.
        All public APIs (OpenWeatherMap, Open-Meteo, SoilGrids, Nominatim) use HTTPS
        and are not affected by this config.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator loopback -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- Common LAN ranges for local development -->
        <domain includeSubdomains="false">192.168.1.1</domain>
        <domain includeSubdomains="false">192.168.0.1</domain>
        <domain includeSubdomains="false">10.0.0.1</domain>
        <!-- localhost -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!-- All other traffic must use HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
